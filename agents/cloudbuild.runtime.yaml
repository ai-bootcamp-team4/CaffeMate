steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - agents/Dockerfile.runtime
      - --tag
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}

options:
  logging: CLOUD_LOGGING_ONLY